export const wordRepeated = (string: string) => {
  const inputs =string
  for(let i = 0; i < inputs.length; i++) 
  for (const word of inputs) {
   
 return inputs
};

console.log(wordRepeated("blablaa"))

}